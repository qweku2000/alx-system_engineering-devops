#!/bin/bash

# Get the value of the BINARY environment variable
BINARY=$BINARY

# Convert the binary number to decimal using bc
DECIMAL=$(echo "ibase=2; $BINARY" | bc)

# Print the decimal number
echo "$DECIMAL"
